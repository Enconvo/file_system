---
name: file_system
description: >
  File system tools for AI agents: read, write, and edit files, search file contents with ripgrep (grep), and find files by glob patterns across text, documents, images, audio, and video.
metadata:
  author: Enconvo
  version: "0.0.169"
---

## API Reference

Just use the `local_api` tool to request these APIs.

